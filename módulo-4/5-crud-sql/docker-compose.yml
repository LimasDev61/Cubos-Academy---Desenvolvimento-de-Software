services:
  postgres:
    container_name: aula_crud_sql
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=aula_crud_sql
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    ports:
      - 5433:5432
