/* Testando o código do condicional-1-IF.js, agora em TSC */

// Constante IDADE1;
const IDADE1: number = 32;

// Palavra reservada IF(SE) - condicional - verifica se a condição é verdadeira;
if(IDADE1 >= 18) {

    console.log('Pode beber - Maior de idade: ' + IDADE1);

}

// Caso o IF(SE) não atenda a condição, a saída só será essa;
console.log('Fim do Código');