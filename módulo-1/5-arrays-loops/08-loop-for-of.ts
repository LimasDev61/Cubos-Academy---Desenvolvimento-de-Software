// Array[4 INDICES] com 5 elementos;
const FRUTAS: string[] = ['banana', 'abacaxi', 'maçã', 'pera', 'uva'];

// Criando o loop FOR OF para percorrer os elementos do array;
for(let item of FRUTAS) {
    // utilizando a variável item, para pecorrer os indices do array;
    console.log(item);
}