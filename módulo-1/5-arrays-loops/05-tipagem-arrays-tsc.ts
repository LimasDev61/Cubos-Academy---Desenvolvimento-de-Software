// Array vazio;
let carros: string[] = [];

// Adicionando elementos ao array vazio;
carros[carros.length] = 'Gol';
carros[carros.length] = 'Palio';
carros[carros.length] = 'Opala';

// Escrevendo os elementos adicionados;
console.log(carros);

