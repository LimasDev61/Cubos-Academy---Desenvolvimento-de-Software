// Código em TS

// Variáveis do tipo boolean e number
const TEM_INGRESSO1: boolean = true;
const AGE1: number = 17;

// Operador lógico AND: testa se todas as condições são verdadeiras
if(TEM_INGRESSO1 && AGE1 > 16) {
    console.log('Pode entrar na festa!');
}
else {
    console.log('Não pode entrar!');
}