// Array[4 INDICES] com 5 elementos;
const FRUTAS: string[] = ['banana', 'abacaxi', 'maçã', 'pera', 'uva'];

// Criando o loop WHILE para percorrer os elementos do array;
let i: number = 0;
while(i < FRUTAS.length) {
    // utilizando a variável i, para pecorrer os indices do array;
    console.log(FRUTAS[i]);
    i++; // incrementando + 1 para i, até chegar no valor final do array;
}